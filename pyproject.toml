[project]
name = "manimations"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David Pauli", email = "bepeppered@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "manim>=0.21.0",
]

[project.scripts]
manimations = "manimations:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
